import React from 'react';
import logo from '../../../assets/images/logo.jpg';

console.log(logo);
const Logo = () => {
    return (
        <img src={logo} alt="logo" />
    );
}

export default Logo;